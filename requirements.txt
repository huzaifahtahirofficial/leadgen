# Nestick runtime dependencies.
# Only httpx is strictly required; the rest improve the experience.

httpx[http2]>=0.27      # async HTTP/2 engine  (required)

rich>=13.0              # colourful CLI output and live dashboard
openpyxl>=3.1           # styled .xlsx export
orjson>=3.9             # faster JSON encoding
brotli>=1.1             # brotli-compressed responses

# Optional: a true native desktop window instead of a Chrome app window.
# Uncomment if you want it (needs a system webview: WebKitGTK / WebView2 / Cocoa).
# pywebview>=5.0

# Optional: centralized authentication (CENTRAL_AUTH_GUIDE.md) — login page +
# JWT bearer checks against AUTH_MONGODB_URI. The server runs without these.
# pymongo>=4.6
# bcrypt>=4.0
# PyJWT>=2.8
